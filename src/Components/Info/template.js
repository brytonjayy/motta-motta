import React from 'react';
import { Flex, Box } from 'rebass';

export const Headering = ( prop ) => {
    return (
        <h1>{prop}</h1>
    )
}

export const Line = (content) => {
    return (
        <p>{content}</p>
    )
}
